<UserControl x:Class="AnimationEditor.Views.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Views="clr-namespace:AnimationEditor.Views"
             xmlns:ViewModels="clr-namespace:AnimationEditor.ViewModels"
             xmlns:p="clr-namespace:AnimationEditor.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <InkCanvas Grid.Row="0" EditingMode="{Binding EditorTools.EditingMode}" Strokes="{Binding AnimationTimelineViewModel.SelectedFrame.StrokeCollection}" DefaultDrawingAttributes="{Binding EditorTools.DrawingAttributes}"/>
        <InkCanvas Grid.Row="0" IsHitTestVisible="False"  Background="Transparent" Strokes="{Binding AnimationTimelineViewModel.PreviousFrameStrokes}">
            <InkCanvas.Style>
                <Style TargetType="{x:Type InkCanvas}">
                    <Style.Setters>
                        <Setter Property="Visibility" Value="Visible"/>
                    </Style.Setters>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsShowPreviousOnionSkinsEnabled, Source={x:Static p:Settings.Default}}" Value="False">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </InkCanvas.Style>
        </InkCanvas>
        <InkCanvas Grid.Row="0" IsHitTestVisible="False"  Background="Transparent" Strokes="{Binding AnimationTimelineViewModel.NextFrameStrokes}" >
            <InkCanvas.Style>
                <Style TargetType="{x:Type InkCanvas}">
                    <Style.Setters>
                        <Setter Property="Visibility" Value="Visible"/>
                    </Style.Setters>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsShowNextOnionSkinsEnabled, Source={x:Static p:Settings.Default}}" Value="False">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </InkCanvas.Style>
        </InkCanvas>
        <Views:AnimationTimelineView Grid.Row="1" DataContext="{Binding AnimationTimelineViewModel}"/>
        <!--<Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="Append Frame" Command="{Binding AppendFrame}" Width="80" Height="30"/>
            <ListBox Grid.Row="1" ItemsSource="{Binding Frames}" SelectedItem="{Binding SelectedFrame, Mode=TwoWay}">
                <ListBox.Resources>
                    <DataTemplate DataType="{x:Type ViewModels:FrameViewModel}">
                        <Grid>
                            <Grid.LayoutTransform>
                                <ScaleTransform CenterX="0.5" CenterY="0.5" ScaleX="0.25" ScaleY="0.25"/>
                            </Grid.LayoutTransform>
                            <InkCanvas EditingMode="None" Strokes="{Binding StrokeCollection}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.Resources>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Grid>-->
    </Grid>
</UserControl>
